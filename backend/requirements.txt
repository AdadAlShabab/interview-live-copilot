# ============================================================
# Interview Copilot — Python Dependencies
# ============================================================
# Install with:
#   pip install -r requirements.txt
#
# All packages are free/open-source. No paid API SDKs.
# ============================================================

# ── Google Gemini AI (required) ────────────────────────────
google-genai>=1.0.0

# ── Web Framework ──────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ── Configuration ──────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# ── Resume Parsing ─────────────────────────────────────────
pdfplumber>=0.11.0          # Primary PDF parser (handles tables well)
pymupdf>=1.24.0             # Fallback PDF parser (fitz)
python-docx>=1.1.0          # DOCX parsing
markdown>=3.7               # Markdown parsing

# ── Database ───────────────────────────────────────────────
sqlalchemy>=2.0.0           # ORM
aiosqlite>=0.20.0           # Async SQLite driver

# ── Vector Search (local, free) ────────────────────────────
faiss-cpu>=1.8.0            # FAISS vector index (CPU version)
sentence-transformers>=3.0.0  # Local embeddings (all-MiniLM-L6-v2)

# ── Audio + Transcription ──────────────────────────────────
faster-whisper>=1.0.0       # Local speech-to-text (free, offline)
sounddevice>=0.5.0          # Cross-platform microphone capture
numpy>=1.26.0               # Audio buffer processing

# ── Utilities ──────────────────────────────────────────────
httpx>=0.27.0               # Async HTTP client
python-multipart>=0.0.12    # File upload support for FastAPI
aiofiles>=24.1.0            # Async file I/O

# ── Desktop Wrapper (Phase 9) ──────────────────────────────
pywebview>=5.1              # Native WebView desktop wrapper

# ── Testing ────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0               # TestClient for FastAPI
